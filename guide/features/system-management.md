# 系统管理

## 用户管理

用户管理用于管理系统用户账号和权限。

![系统管理概览](/images/screenshots/system-management.png)

### 用户状态

| 状态 | 说明 |
|------|------|
| **正常** | 用户可正常登录系统并使用相应权限功能 |
| **待审核** | 新注册用户，等待管理员审核通过后方可登录 |
| **已禁用** | 账号被管理员禁用，无法登录系统 |

### 审核流程

用户注册审核流程如下：

1. **用户注册**：新用户通过注册页面提交账号信息
2. **待审核状态**：注册成功后，账号自动进入「待审核」状态
3. **管理员审核**：管理员在「用户管理」页面查看待审核用户列表
4. **审核操作**：管理员可「通过」或「驳回」注册申请，驳回需填写原因
5. **结果通知**：
   - 审核通过：用户收到通知，账号状态变更为「正常」，可登录系统
   - 审核驳回：用户收到通知及驳回原因，可根据原因修改后重新提交注册

### 角色权限

系统采用 RBAC（基于角色的访问控制）权限模型：

| 角色 | 权限范围 |
|------|----------|
| **管理员** | 全部功能，包括系统设置、用户管理、数据管理、所有模块的增删改查 |
| **运维人员** | 设备管理（含导入导出、端口管理、接线管理）、工单处理（接单、处理、完成） |
| **普通用户** | 查看权限（查看设备、工单、仪表盘等），可创建工单 |

### 角色管理

- **自定义角色**：支持管理员自定义角色，为不同岗位配置专属权限组合
- **权限粒度**：权限控制到页面级和操作级（新增、编辑、删除、查看、导入、导出）
- **角色分配**：在用户编辑页面勾选角色，支持一个用户拥有多个角色

## 系统设置

系统设置用于配置系统全局参数，包含「基本设置」「邮件服务」「关于系统」三个标签页。

### 基本设置

基本设置包含站点信息、公司信息、安全设置等通用配置。

#### 站点信息

| 设置项 | 说明 |
|--------|------|
| **站点名称** | 系统浏览器标签页标题和页面左上角显示的名称 |
| **Logo** | 系统左上角显示的 Logo 图片，建议尺寸 120×40px，支持 PNG/SVG 格式 |

#### 安全设置

| 设置项 | 说明 |
|--------|------|
| **空闲超时** | 用户无操作后自动退出登录的时间，可选范围：15分钟、30分钟、60分钟、永不超时 |
| **背景图片** | 登录页的背景图片，支持 JPG/PNG 格式 |
| **维护模式** | 开启后系统仅管理员可登录，其他用户访问时显示维护提示页面 |
| **登录限制** | 可限制指定 IP 段或仅允许内网 IP 访问系统登录页 |

### 邮件服务（管理员专属）

> v2.3.0 新增，仅管理员可见此标签页。

邮件服务用于配置 SMTP 邮件发送，支持发送验证码邮件、测试邮件等功能。

#### 配置项

| 设置项 | 说明 |
|--------|------|
| **SMTP 服务器** | 邮件发送服务器地址（如 smtp.example.com） |
| **端口** | SMTP 服务端口（465/587/25 等） |
| **加密方式** | SSL/TLS 加密方式 |
| **发件人邮箱** | 发件人邮箱地址 |
| **发件人名称** | 邮件发件人显示名称 |
| **SMTP 用户名** | 邮箱登录用户名 |
| **SMTP 密码** | 邮箱登录密码或授权码 |

#### 操作说明

1. 进入「系统管理」→「系统设置」→「邮件服务」标签
2. 填写 SMTP 服务器配置信息
3. 点击「保存」完成配置
4. （可选）点击「发送测试邮件」验证配置是否生效

### 关于系统

关于系统页面提供系统版本信息和相关工具。

#### 系统信息

- **系统名称**：IDC 设备资产管理系统
- **当前版本**：显示当前运行的版本号
- **部署环境**：显示是否运行在 Docker 容器中

#### 在线更新

> v2.3.2 新增。

点击「检查更新」按钮，系统自动检测是否有新版本可用：

- **已是最新**：绿色标识提示已是最新版本
- **发现新版本**：显示当前版本与最新版本对比，并提供更新指引
  - 非 Docker 环境：显示终端命令提示（`cd 项目路径 && node update.js`），支持一键复制
  - Docker 环境：提示拉取最新镜像后重建容器
- 更新日志区域展示版本发布说明

#### 开源许可

> v2.3.2 新增。

点击「开源许可」按钮，可查看系统前后端所有依赖包的开源许可信息：

- 按许可类型分组统计（MIT、Apache-2.0、ISC 等）
- 列表展示：依赖名称、版本、许可类型、运行/开发分类、项目主页
- 支持分页浏览

### 操作说明

1. 进入「系统管理」→「系统设置」页面
2. 通过左侧标签导航切换不同设置模块
3. 修改配置项后点击「保存」按钮
4. 部分设置（如站点名称、Logo、空闲超时）保存后立即生效
5. 维护模式和登录限制等安全设置保存后，下一次登录时生效
